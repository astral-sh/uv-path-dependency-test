[project]
name = "package1"
version = "0.1.0"
readme = "README.md"
authors = [
    { name = "Charlie Marsh", email = "charlie@astral.sh" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
